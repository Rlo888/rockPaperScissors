// access values of an array
const arr = [1, 2, 3];
const firstValue = arr[0];
const second = arr[1];

// access values of an object
const obj = { a: 1, b: 2, c: 3 };
const aValue = obj.a;
const bValue = obj.b;
const otherWayToGetA = obj["a"];
const otherWayToGetB = obj["b"];


const mathRandom = Math.random();
  const mathRandomTimes3 = mathRandom * 3;
  const mathRandomTimes3RoundedDown = Math.floor(mathRandomTimes3);
  console.log({ mathRandom, mathRandomTimes3, mathRandomTimes3RoundedDown });
